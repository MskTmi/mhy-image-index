# 基础依赖
dghs-imgutils>=0.19.0
Pillow>=10.0.0
huggingface_hub>=0.24,<2.0
hf-xet>=1.0,<2.0
requests>=2.28.0
pandas>=2.0.0
numpy>=1.24.0

# === GPU 推理（推荐）===
# 有 NVIDIA 显卡时装这套，自动利用 GPU 加速
onnxruntime-gpu>=1.17.0
# CUDA 12 运行时（pip 版，无需装 CUDA Toolkit）
# tag_image.py 启动时会自动发现这些 DLL
# onnxruntime-gpu 的 CUDAExecutionProvider 在 Windows 上需要以下全部库：
nvidia-cublas-cu12
nvidia-cudnn-cu12
nvidia-cuda-runtime-cu12
nvidia-cuda-nvrtc-cu12
nvidia-cufft-cu12
nvidia-curand-cu12
nvidia-cusolver-cu12
nvidia-cusparse-cu12

# === CPU 推理（备选）===
# 无 NVIDIA 显卡时，把上面 onnxruntime-gpu 与 nvidia-* 注释掉，改用：
# onnxruntime>=1.17.0